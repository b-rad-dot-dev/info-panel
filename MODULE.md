# Module Interface

The constructor of the module will receive the following (in order):

* The container to place the module's HTML
* The config for your module
* An API object of functions that may be called, for example
  * `log` - Sends a log message to the info-panel application